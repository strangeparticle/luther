package com.strangeparticle.luther.cmp

import androidx.compose.ui.input.pointer.PointerIcon

// Touch platforms have no hover cursor, so the resize thumb falls back to the default icon.
internal actual val aiChatVerticalResizePointerIcon: PointerIcon = PointerIcon.Default
